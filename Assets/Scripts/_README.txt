The script file in the Editor folder should not be taken out. Only take 
the script out of the folder if you already have an editor folder in your
project.

ELSE, just drop the Editor folder into your project anywhere, the script in
there just has to stay in that folder for it to work.